# TCC — Vinícius Abreu | SENAI CIMATEC 2026
# Protocolo de Diagnóstico Estatístico para Séries Temporais

# ── Manipulação de dados ──────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ── Testes estatísticos e modelos clássicos ───────────────────
statsmodels>=0.14       # ADF, KPSS, ARIMA, SARIMA, ACF, PACF
arch>=6.0               # BDS test, ARCH test, GARCH, EGARCH
pmdarima>=2.0           # Auto-ARIMA (benchmark cego)
hurst>=0.0.5            # Expoente de Hurst (memória longa)

# ── Deep Learning (modelos não-lineares) ─────────────────────
tensorflow>=2.13        # LSTM, GRU
keras>=2.13

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0

# ── Visualização ─────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# ── Notebooks e utilitários ───────────────────────────────────
jupyter>=1.0
ipykernel>=6.0
tqdm>=4.65
requests>=2.31

# ── RAG (Streamlit + Agno) ───────────────────────────────────
streamlit>=1.30
agno>=0.1
pypdf>=3.0
sentence-transformers>=2.2
chromadb>=0.4
